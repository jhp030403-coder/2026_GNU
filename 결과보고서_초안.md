# 「제5회 AI·공공데이터 활용 및 창업 경진대회 공모전」
# AI·데이터를 활용한 서비스 발굴 부문 결과보고서

## AI·데이터를 활용한 서비스 발굴

### 서비스명

전세 적정가 및 위험 매물 판단 서비스

### 한 줄 소개

공공 주거·거래 데이터를 기반으로 특정 전세 매물의 적정 보증금 범위와 위험도를 예측하여, 임차인이 계약 전 과도한 보증금·이상 매물 여부를 사전에 확인할 수 있도록 돕는 AI 기반 의사결정 지원 서비스이다.

---

## 기관선정

한국토지주택공사

본 서비스는 전세 임차인의 주거 안정과 공공 주거지원 정책의 실효성 제고를 목표로 한다. 한국토지주택공사는 공공임대, 전세임대, 주거복지, 주택시장 안정과 밀접한 역할을 수행하고 있으므로, 전세 보증금의 적정성 판단 및 위험 매물 사전 탐지 서비스와 가장 높은 연관성을 가진 기관으로 판단하였다.

특히 본 프로젝트는 지역별·주택유형별 전세 거래 수준, 매매가 대비 전세가, 주변 시세 대비 보증금 수준 등을 종합적으로 분석하여 임차인이 체감할 수 있는 주거 위험 정보를 제공하는 데 목적이 있다. 이는 한국토지주택공사의 주거복지 서비스 고도화, 전세임대 지원 대상자의 정보 비대칭 완화, 공공 데이터 기반 주택시장 분석 체계 강화와 연결될 수 있다.

---

## 1) AI 활용

본 프로젝트는 전세 매물의 적정 보증금과 위험도를 산출하기 위해 지도학습, 하이퍼파라미터 최적화, 앙상블, 비지도학습을 함께 활용하였다.

첫째, 지도학습 기반 회귀 모델을 활용하였다. 주요 예측 대상은 주변 전세 중위가 대비 대상 매물의 보증금 수준을 나타내는 비율형 지표이며, 이를 로그 변환 및 이상치 완화 처리한 `log1p_lh_vs_rent_median_ratio_winsor`를 타깃 변수로 설정하였다. 모델은 자치구, 계약연월, 주택유형, 면적, 방 수, 가구원 수, 주변 전월세 거래 통계, 매매가 통계 등 다양한 특성을 입력받아 적정 보증금 비율을 예측한다.

둘째, CatBoost, LightGBM, XGBoost, DNN, Stacking, Weighted Ensemble 등 복수의 모델을 비교하였다. 특히 범주형 변수가 많은 부동산 데이터 특성을 고려하여 CatBoost를 핵심 모델로 사용하였고, Optuna를 활용하여 반복 횟수, 학습률, 트리 깊이, 정규화 계수, 부트스트랩 방식 등 주요 하이퍼파라미터를 자동 탐색하였다.

셋째, KMeans와 IsolationForest 기반 비지도학습을 활용하여 매물이 일반적인 거래 패턴에서 얼마나 벗어나는지 계산하였다. 지도학습 모델이 “적정 보증금”을 예측한다면, 비지도학습 모델은 “입력 매물이 정상적인 시장 패턴에 가까운지”를 보조적으로 판단한다. 이를 통해 단순 가격 예측을 넘어 이상 매물 가능성, 희소한 주택유형, 비정상적 입력값 등에 대한 위험 신호를 제공한다.

넷째, 최종 서비스에서는 예측값을 사용자가 이해하기 쉬운 형태로 변환하였다. 모델 출력값은 예측 적정 보증금, 95% 예측 구간, 입력 보증금과 적정가의 차이, 가격 이상 점수, 위험 라벨로 재구성된다. 사용자는 복잡한 모델 지표가 아니라 “적정 매물”, “주의 매물”, “위험 매물”과 같은 판단 결과를 확인할 수 있다.

활용한 주요 AI 및 알고리즘은 다음과 같다.

- CatBoost Regressor: 범주형 변수를 포함한 전세 적정가 예측
- Optuna: CatBoost 하이퍼파라미터 자동 최적화
- LightGBM, XGBoost: 비교 모델 및 앙상블 후보
- Stacking/Weighted Ensemble: 복수 모델 예측값 결합
- KMeans: 유사 매물 군집화 및 군집 거리 산출
- IsolationForest: 이상치 및 비정상 패턴 탐지
- DNN: 딥러닝 기반 회귀 모델 실험
- Conformal Prediction 방식의 잔차 분위수 활용: 예측 불확실성 및 95% 범위 산출

---

## 2) 활용 데이터

본 프로젝트는 주택 및 전세 거래와 관련된 공공·가공 데이터를 활용하였다. 프로젝트 내 모델링 데이터는 `data/modeling/modeling_dataset_preprocessed.csv`를 중심으로 구성되어 있으며, 최종 모델 학습에는 전처리 및 품질 검사를 거친 데이터가 사용되었다.

활용 데이터의 주요 구성은 다음과 같다.

| 구분 | 주요 변수 | 활용 목적 |
|---|---|---|
| 지역 정보 | 자치구 코드, 자치구명 | 지역별 전세 수준 차이 반영 |
| 시점 정보 | 계약연월, 연도 | 시기별 시장 변화 반영 |
| 주택 정보 | 주택유형, 상세유형, 면적, 방 수, 가구원 수 | 매물 특성 기반 적정가 예측 |
| 전월세 거래 정보 | 전세 거래 수, 월세 거래 수, 주변 전세 보증금 중위값·평균값, 면적당 보증금 | 주변 시세 수준 산출 |
| 매매 거래 정보 | 매매 거래 수, 매매가 중위값·평균값, 면적당 매매가 | 전세가와 매매가의 관계 반영 |
| 품질 및 이상치 변수 | 면적 결측·과소·과대 여부, 방 수 결측 여부, 거래 정보 신뢰도 플래그 | 모델 입력 신뢰도 향상 |
| 파생 변수 | 지역-주택유형 조합, 지역-월 조합, 주택유형-월 조합 | 지역·시점·유형 간 상호작용 반영 |

모델 학습 데이터는 총 64,432건으로 구성되며, 학습·검증·테스트 데이터는 자치구, 계약연월, 주택유형을 기준으로 그룹화하여 분할하였다. 분할 비율은 학습 70%, 검증 15%, 테스트 15%이며, 실제 분할 결과는 다음과 같다.

| 데이터 구분 | 행 수 |
|---|---:|
| 학습 데이터 | 45,477 |
| 검증 데이터 | 9,881 |
| 테스트 데이터 | 9,074 |

연도별로는 2022년부터 2025년까지의 데이터가 포함되어 있으며, 동일한 지역·시점·유형 조합이 학습과 테스트에 무작위로 섞이는 문제를 줄이기 위해 연도 기반 그룹 분할 전략을 적용하였다. 이를 통해 실제 서비스 상황에서 새로운 조합 또는 시점의 매물을 예측하는 상황에 더 가까운 평가가 가능하도록 하였다.

---

## 3) 필요성 및 목적

전세 계약은 일반 소비자에게 매우 큰 금액이 오가는 의사결정이다. 그러나 임차인이 계약 전에 확인할 수 있는 정보는 제한적이며, 같은 자치구 안에서도 주택유형, 면적, 계약 시점, 주변 거래량, 매매가 수준에 따라 적정 보증금은 크게 달라진다. 특히 전세사기, 깡통전세, 주변 시세 대비 과도한 보증금 요구 등의 문제가 사회적으로 커지면서, 임차인이 계약 전에 객관적 기준으로 위험을 확인할 수 있는 도구의 필요성이 높아지고 있다.

기존 부동산 플랫폼은 매물 정보와 단순 시세를 제공하는 데 초점이 맞춰져 있는 경우가 많다. 그러나 사용자가 실제로 알고 싶은 것은 “이 매물이 비싼가?”, “주변 시세와 비교했을 때 안전한가?”, “예측 가능한 적정 보증금 범위는 어디까지인가?”이다. 본 서비스는 이러한 질문에 답하기 위해 공공 데이터와 AI 모델을 결합하였다.

본 프로젝트의 목적은 다음과 같다.

첫째, 전세 매물의 적정 보증금을 데이터 기반으로 예측한다. 사용자가 자치구, 주택유형, 면적, 방 수, 보증금 등 기본 정보를 입력하면, 모델은 주변 전세 중위가와 매매 시세를 반영하여 적정 보증금 수준을 산출한다.

둘째, 예측값의 불확실성을 함께 제공한다. 단일 금액만 제시하면 사용자가 이를 절대적인 정답으로 오해할 수 있으므로, 본 서비스는 95% 예측 구간을 함께 제공한다. 이를 통해 시장 변동성, 데이터 부족, 지역별 특성 차이를 고려한 현실적인 판단이 가능하다.

셋째, 위험 매물을 직관적으로 판단할 수 있도록 한다. 입력 보증금이 예측 적정가 대비 과도하게 높거나 낮은 경우, 또는 해당 매물이 일반적인 거래 패턴에서 벗어난 경우 위험 점수를 높게 산출한다. 최종적으로 안전, 적정, 주의, 위험과 같은 라벨을 제공하여 비전문가도 결과를 이해할 수 있도록 하였다.

넷째, 공공기관의 주거복지 의사결정을 보조한다. 한국토지주택공사와 같은 기관은 전세임대 지원, 주거 취약계층 상담, 지역별 주거 위험 모니터링 등에 본 서비스를 활용할 수 있다. 개별 임차인에게는 계약 전 확인 도구로, 기관에게는 위험 매물 탐지 및 정책 지원 도구로 확장 가능하다.

---

## 4) 세부 내용

### 4-1. 서비스 개요

본 서비스는 “전세 적정가 및 위험 매물 판단”을 핵심 기능으로 한다. 사용자는 웹 화면에서 자치구, 주택유형, 계약연월, 전용면적, 방 수, 가구원 수, 현재 제시된 전세 보증금 등을 입력한다. 서비스는 입력값을 모델에 전달하고, 최종적으로 다음 결과를 제공한다.

- 예측 적정 전세 보증금
- 95% 적정 보증금 하한·상한
- 현재 제시 보증금과 예측 적정가의 차이
- 주변 전세 중위가 기준
- 가격 이상 점수
- 이상 패턴 점수
- 최종 위험 라벨

웹 서비스 화면은 `src/frontend/app.py`로 구현되어 있으며, Python 내장 HTTP 서버를 사용하여 로컬에서 실행할 수 있다. 실행 후 브라우저에서 `http://127.0.0.1:8501`에 접속하면 입력 폼과 예측 결과 화면을 확인할 수 있다.

### 4-2. 전체 처리 흐름

서비스의 전체 흐름은 다음과 같다.

1. 공공·거래 데이터 수집 및 통합
2. 주소, 주택유형, 면적, 거래가격 등 주요 변수 정제
3. 지역·시점·주택유형별 주변 시세 통계 생성
4. 모델 학습용 타깃 변수와 파생 변수 생성
5. 학습·검증·테스트 데이터 분할
6. 여러 모델 학습 및 성능 비교
7. 최적 모델 선정 및 최종 모델 저장
8. 웹 입력값을 모델 입력 형식으로 변환
9. 적정 보증금과 위험 점수 산출
10. 사용자 화면에 결과 제공

### 4-3. 데이터 전처리 및 특징 생성

전처리 단계에서는 거래 데이터의 결측값, 비정상 면적, 과도한 방 수, 거래량 부족, 매매 정보 결측 등을 식별하였다. 이러한 값들은 단순히 제거하지 않고, 가능한 경우 보정값과 품질 플래그를 함께 생성하였다. 예를 들어 면적이 결측이거나 0인 경우 `area_missing_or_zero`, 면적이 지나치게 작은 경우 `area_too_small`, 매매 정보가 부족한 경우 `sale_feature_missing` 등의 변수를 생성하여 모델이 데이터 신뢰도를 학습할 수 있도록 하였다.

또한 부동산 가격은 지역, 시점, 주택유형의 상호작용에 크게 영향을 받는다. 따라서 단일 변수만 사용하는 대신 다음과 같은 조합형 범주 변수를 추가하였다.

- 자치구 + 주택유형
- 자치구 + 계약연월
- 주택유형 + 계약연월
- 주택 세부유형 + 계약연월

이 조합 변수는 CatBoost 모델에서 범주형 변수로 처리되어, 특정 지역·주택유형·시점 조합에서 나타나는 전세 가격 패턴을 학습하는 데 활용되었다.

### 4-4. 모델 학습 및 성능 비교

본 프로젝트에서는 여러 모델을 실험하여 최종 모델을 선정하였다. 기본 선형·트리 모델, CatBoost, XGBoost, LightGBM, Stacking, Supervised Ensemble, DNN 등이 비교 대상에 포함되었다. 성능 평가는 검증 데이터와 테스트 데이터에서 RMSE, MAE, R2를 기준으로 수행하였다.

핵심 모델인 CatBoost Optuna 모델의 테스트 성능은 다음과 같다.

| 모델 | 테스트 RMSE | 테스트 MAE | 테스트 R2 |
|---|---:|---:|---:|
| CatBoost Optuna | 0.1815 | 0.1308 | 0.9309 |

R2가 약 0.931로 나타나, 모델이 테스트 데이터의 변동성을 상당 부분 설명하는 것으로 확인되었다. 또한 RMSE와 MAE를 함께 확인하여 큰 오차와 평균적 오차를 동시에 검토하였다.

최적 CatBoost 모델의 주요 하이퍼파라미터는 다음과 같다.

| 항목 | 값 |
|---|---:|
| bootstrap_type | MVS |
| iterations | 1855 |
| learning_rate | 0.0675 |
| depth | 6 |
| l2_leaf_reg | 7.8105 |
| border_count | 192 |
| min_data_in_leaf | 67 |
| subsample | 0.7394 |

해당 모델은 Optuna를 통해 100회 탐색된 결과이며, CatBoost의 범주형 변수 처리 능력과 하이퍼파라미터 최적화의 장점을 함께 활용하였다.

### 4-5. 모델 선택 이유 및 작동 원리

본 프로젝트에서는 최종 서비스 모델로 CatBoost 기반 회귀 모델을 선택하였다. 전세 매물 데이터는 자치구, 계약연월, 주택유형, 상세유형처럼 범주형 변수가 많고, 지역·시점·유형의 조합에 따라 가격 패턴이 크게 달라진다. 일반적인 선형회귀처럼 변수 간 관계를 단순한 직선 형태로 가정하는 모델은 이러한 복잡한 상호작용을 충분히 반영하기 어렵다. 반면 CatBoost는 범주형 변수를 효과적으로 처리하면서 비선형 관계와 변수 간 상호작용을 학습할 수 있어 본 프로젝트의 데이터 특성과 잘 맞는다.

CatBoost는 Gradient Boosting Decision Tree 계열 모델이다. 여러 개의 의사결정나무를 순차적으로 학습하면서 이전 나무가 잘 맞히지 못한 오차를 다음 나무가 보완하는 방식으로 작동한다. 첫 번째 나무가 대략적인 예측을 수행하면, 이후 나무들은 잔차 또는 손실 함수의 기울기 정보를 이용해 예측 오류를 줄이는 방향으로 추가된다. 최종 예측값은 여러 나무의 예측을 합산한 결과이다.

CatBoost의 핵심 장점은 범주형 변수 처리 방식이다. 일반적인 머신러닝 모델은 범주형 변수를 One-Hot Encoding으로 바꾸는 경우가 많다. 그러나 자치구, 주택유형, 계약연월, 지역-유형 조합처럼 종류가 많은 변수는 One-Hot Encoding을 적용하면 차원이 커지고 희소한 데이터가 많아진다. CatBoost는 범주형 변수의 통계적 정보를 활용하는 방식으로 이를 처리하여, 범주형 변수의 정보 손실과 과적합 위험을 줄일 수 있다.

또한 본 프로젝트에서는 Optuna를 사용하여 CatBoost의 하이퍼파라미터를 자동으로 탐색하였다. 하이퍼파라미터는 모델 학습 전 사람이 정해야 하는 설정값으로, 예를 들어 트리 개수, 학습률, 트리 깊이, 정규화 강도 등이 있다. Optuna는 여러 조합을 반복 실험하면서 검증 RMSE가 낮아지는 방향으로 좋은 설정을 찾아간다. 이를 통해 사람이 임의로 설정하는 것보다 더 안정적인 모델 성능을 확보할 수 있었다.

비교 모델로 LightGBM과 XGBoost도 사용하였다. 두 모델 역시 Gradient Boosting Decision Tree 계열로, 수치형·범주형 변수가 섞인 표 형태 데이터에서 강력한 성능을 보이는 대표적인 알고리즘이다. LightGBM은 학습 속도가 빠르고 대용량 데이터 처리에 유리하며, XGBoost는 정규화와 안정적인 부스팅 구조로 널리 사용된다. 이 두 모델을 함께 실험한 이유는 CatBoost의 성능이 실제로 우수한지 비교 기준을 확보하기 위해서이다.

DNN 모델도 실험하였다. DNN은 범주형 변수를 embedding으로 변환하고 수치형 변수와 결합하여 다층 신경망으로 예측을 수행한다. 범주형 embedding은 자치구나 주택유형 같은 값을 저차원 벡터로 표현하여, 서로 유사한 패턴을 가진 범주가 비슷한 표현을 갖도록 학습할 수 있다. 다만 본 프로젝트처럼 데이터가 표 형태이고 범주형·수치형 feature가 명확한 경우에는 트리 기반 부스팅 모델이 더 안정적인 성능을 보이는 경우가 많아, 최종 모델은 CatBoost를 중심으로 구성하였다.

비지도학습 모델인 KMeans와 IsolationForest는 가격 예측 자체보다는 위험도 판단을 보완하기 위해 사용하였다. KMeans는 입력 매물을 몇 개의 유사한 군집으로 나누고, 매물이 해당 군집 중심에서 얼마나 떨어져 있는지 계산한다. 군집 중심에서 멀리 떨어진 매물은 일반적인 거래 패턴과 다를 가능성이 있다. IsolationForest는 데이터를 무작위 기준으로 반복 분할했을 때 얼마나 빨리 고립되는지를 기반으로 이상치를 탐지한다. 일반적이지 않은 매물일수록 적은 분할만으로 고립되므로 이상 점수가 높아진다.

따라서 본 프로젝트의 모델 구조는 다음과 같이 역할을 나눌 수 있다.

| 모델 | 선택 이유 | 작동 원리 | 본 프로젝트에서의 역할 |
|---|---|---|---|
| CatBoost | 범주형 변수가 많은 부동산 데이터에 적합 | 이전 예측 오차를 줄이는 트리를 순차적으로 추가 | 최종 적정 보증금 예측 |
| Optuna | 모델 성능을 높이기 위한 자동 탐색 필요 | 여러 하이퍼파라미터 조합을 시도해 검증 성능이 좋은 조합 탐색 | CatBoost 최적 설정 탐색 |
| LightGBM | 빠른 학습과 강한 성능 | Gradient Boosting Tree 기반 | 비교 모델 및 앙상블 후보 |
| XGBoost | 안정적인 트리 부스팅 성능 | 정규화가 포함된 Gradient Boosting | 비교 모델 및 앙상블 후보 |
| DNN | 범주형 embedding 실험 가능 | embedding + 다층 신경망 | 딥러닝 기반 대안 모델 |
| Stacking/Ensemble | 단일 모델의 한계 보완 | 여러 모델 예측값을 결합 | 성능 비교 및 후보 조합 |
| KMeans | 유사 매물군 파악 | 가까운 데이터끼리 군집화 | 일반 패턴과의 거리 계산 |
| IsolationForest | 이상 매물 탐지 | 쉽게 고립되는 데이터를 이상치로 판단 | 이상 패턴 위험도 산출 |

### 4-6. 분석 시 유용한 점

본 프로젝트의 분석 방식은 단순히 예측 정확도만 높이는 것이 아니라, 실제 서비스와 정책 활용에 필요한 해석 가능성과 위험 판단 가능성을 함께 고려했다는 점에서 유용하다.

첫째, 지역·시점·주택유형별 차이를 반영할 수 있다. 전세 보증금은 전국 또는 서울 전체의 평균값만으로 판단하기 어렵다. 같은 금액이라도 어느 자치구인지, 어떤 주택유형인지, 어느 시점의 계약인지에 따라 적정성 판단이 달라진다. 본 모델은 `gu_name`, `ym`, `property_type`뿐 아니라 `gu_property`, `gu_ym`, `property_ym` 같은 조합 feature를 사용하여 이러한 상호작용을 반영한다.

둘째, 주변 시세와 매매 시세를 동시에 고려할 수 있다. `rent_deposit_median`, `rent_deposit_per_m2_median`, `sale_price_median`, `sale_price_per_m2_median` 등의 feature를 활용함으로써 단순히 입력 매물 자체만 보는 것이 아니라 주변 전세 시장과 매매 시장의 정보를 함께 분석한다. 이는 전세가가 매매가에 비해 과도한지, 주변 전세 수준과 비교해 비정상적인지를 판단하는 데 유용하다.

셋째, 데이터 품질 문제를 모델에 반영할 수 있다. 부동산 데이터에는 면적 결측, 과도하게 큰 면적, 방 개수 오류, 거래량 부족 등 다양한 품질 문제가 존재한다. 본 프로젝트는 이러한 문제를 제거만 하지 않고 `area_missing_or_zero`, `area_too_large`, `sale_low_confidence`, `rent_feature_missing` 같은 품질 플래그로 변환하였다. 이를 통해 모델은 “시세 정보가 충분한 매물”과 “정보가 부족한 매물”을 구분해서 학습할 수 있다.

넷째, 예측 결과를 금액 단위로 환산할 수 있다. 모델은 비율형 타깃을 예측하지만, 서비스에서는 이를 주변 전세 중위가에 곱하여 원 단위의 적정 보증금으로 제공한다. 따라서 사용자는 RMSE나 로그 비율 같은 모델 지표를 몰라도 “예측 적정가”, “하한”, “상한”을 직접 확인할 수 있다.

다섯째, 예측 불확실성을 함께 제공할 수 있다. 전세 가격은 시장 상황과 개별 매물 조건에 따라 변동성이 크므로 단일 예측값만 제시하면 위험하다. 본 프로젝트는 95% 예측 구간을 함께 제공하여 사용자가 적정 범위를 기준으로 판단할 수 있도록 했다.

여섯째, 이상 매물 판단에 유용하다. KMeans와 IsolationForest를 활용하면 단순히 비싼 매물뿐 아니라 학습 데이터에서 드문 유형의 매물, 일반적인 군집에서 멀리 떨어진 매물도 탐지할 수 있다. 이는 허위 매물, 특수 조건 매물, 데이터 입력 오류 가능성을 점검하는 보조 지표로 활용될 수 있다.

일곱째, 공공기관의 의사결정 보조에 활용하기 좋다. 최종 결과가 점수와 라벨로 제공되므로 상담 담당자나 정책 담당자가 빠르게 고위험 매물을 선별할 수 있다. 또한 지역별·유형별 위험 결과가 누적되면 위험 매물 분포를 모니터링하거나 주거복지 정책의 우선순위를 정하는 데 활용할 수 있다.

### 4-7. 활용 방안

본 서비스는 개인 임차인, 공공기관, 지자체, 부동산 상담 현장 등 여러 주체가 활용할 수 있다. 핵심은 복잡한 주택 거래 데이터를 사용자가 이해하기 쉬운 적정가·위험도 정보로 변환하는 것이다.

첫째, 임차인의 계약 전 자가 점검 도구로 활용할 수 있다. 임차인은 관심 매물의 자치구, 주택유형, 계약연월, 면적, 방 개수, 제시 보증금 등을 입력하여 해당 매물이 주변 시세 대비 적정한지 확인할 수 있다. 예측 적정 보증금과 95% 예측 구간을 함께 제공하므로, 단순히 “비싸다/싸다”가 아니라 어느 정도 범위 안에서 판단해야 하는지 확인할 수 있다.

둘째, 전세임대 및 주거복지 상담 보조 도구로 활용할 수 있다. 한국토지주택공사 등 공공기관의 상담 담당자는 신청자가 제시한 매물을 시스템에 입력하여 보증금이 과도한지, 주변 거래 데이터가 충분한지, 비정상 패턴이 있는지 빠르게 검토할 수 있다. 위험 점수가 높은 매물은 추가 서류 확인, 보증보험 가능 여부 확인, 대체 매물 안내 등 후속 절차로 연결할 수 있다.

셋째, 위험 매물 사전 탐지 및 모니터링에 활용할 수 있다. 개별 매물의 위험 판단 결과를 누적하면 특정 지역, 특정 주택유형, 특정 시점에서 위험 점수가 높은 매물이 증가하는 흐름을 파악할 수 있다. 이는 전세사기 예방 캠페인, 주거 취약지역 점검, 정책 지원 우선순위 설정에 활용될 수 있다.

넷째, 부동산 정보 플랫폼과 연계할 수 있다. 기존 플랫폼이 매물 목록과 가격 비교를 제공한다면, 본 서비스는 그 위에 “AI 기반 적정가 판단”과 “위험도 라벨”을 추가하는 방식으로 확장 가능하다. 사용자는 매물을 탐색하는 단계에서 위험 신호를 미리 확인할 수 있고, 플랫폼은 사용자 보호 기능을 강화할 수 있다.

다섯째, 정책 분석 및 연구 자료로 활용할 수 있다. 지역별 예측 오차, 위험 점수, 전세가율 관련 feature를 분석하면 어느 지역에서 시세 왜곡이 크고, 어떤 주택유형에서 정보 비대칭이 큰지 파악할 수 있다. 이는 주거 안정 정책, 보증금 지원 정책, 전세보증 사고 예방 정책의 근거 자료가 될 수 있다.

활용 시나리오는 다음과 같이 정리할 수 있다.

| 활용 주체 | 활용 방식 | 기대되는 결과 |
|---|---|---|
| 일반 임차인 | 계약 전 매물 정보 입력 후 적정가·위험도 확인 | 과도한 보증금 부담 및 위험 계약 예방 |
| 공공기관 상담 담당자 | 전세임대·주거상담 과정에서 매물 위험도 검토 | 고위험 매물 선별 및 추가 확인 절차 지원 |
| 지자체·공공기관 | 지역별 위험 매물 분포 모니터링 | 주거 취약지역 및 위험 유형 조기 파악 |
| 부동산 플랫폼 | 매물 상세 페이지에 위험도 라벨 제공 | 사용자 보호 및 정보 신뢰도 향상 |
| 연구·정책 담당자 | 위험 점수와 예측 오차를 집계 분석 | 데이터 기반 주거 안정 정책 수립 |

### 4-8. 구체성 및 논리성

본 프로젝트는 아이디어 제안에 그치지 않고, 데이터 처리부터 모델 학습, 최종 예측, 웹 서비스 구현까지 하나의 흐름으로 연결되어 있다. 따라서 서비스 기획의 구체성과 논리성을 다음과 같이 설명할 수 있다.

첫째, 문제 정의가 명확하다. 본 서비스가 해결하려는 문제는 “전세 매물이 주변 시세와 비교해 적정한지, 위험 신호가 있는지 임차인이 사전에 판단하기 어렵다”는 것이다. 이에 따라 모델의 출력도 단순한 가격 예측이 아니라 적정 보증금, 예측 구간, 가격 차이, 위험 점수, 위험 라벨로 구성하였다.

둘째, 타깃 변수 설계가 서비스 목적과 연결된다. 모델은 보증금 절대금액을 직접 예측하기보다 주변 전세 중위가 대비 비율을 예측한다. 전세 보증금은 지역과 주택유형에 따라 절대 수준이 크게 다르므로, 비율형 타깃을 사용하면 지역별 가격 규모 차이를 줄이고 “주변 시세 대비 적정성”이라는 서비스 목적에 더 잘 맞는 예측이 가능하다. 이후 예측 비율에 주변 전세 중위가를 곱해 사용자가 이해할 수 있는 원 단위 적정 보증금으로 변환한다.

셋째, feature 구성이 논리적이다. 지역 정보(`gu_code`, `gu_name`), 시점 정보(`ym`), 주택유형 정보(`property_type`), 물리적 특성(`area_m2`, 방 개수, 세대원 수), 주변 전세 시세(`rent_deposit_median`), 주변 매매 시세(`sale_price_median`), 데이터 품질 플래그(`sale_low_confidence`, `area_too_large`)를 함께 사용한다. 이는 전세 보증금이 지역, 시점, 주택 특성, 주변 시장 가격, 데이터 신뢰도에 의해 결정된다는 부동산 시장의 구조와 일치한다.

넷째, 데이터 누출을 방지하였다. `deposit_won`, `support_won`, `tenant_deposit_won`, 타깃과 직접 연결되는 비율 변수 등은 모델 입력에서 제외하였다. 이러한 변수들을 학습에 사용하면 모델 성능은 높아 보일 수 있으나 실제 서비스에서 사용할 수 없거나 정답을 미리 알려주는 문제가 발생한다. 본 프로젝트는 예측 시점에 활용 가능한 정보 중심으로 feature를 구성하여 실제 적용 가능성을 높였다.

다섯째, 모델 비교 과정이 단계적으로 구성되어 있다. 먼저 기준 모델과 트리 기반 모델을 학습하고, 이후 Optuna로 하이퍼파라미터를 최적화하였다. 추가로 Stacking, Supervised Ensemble, DNN을 실험하여 단일 모델과 결합 모델의 성능을 비교하였다. 최종적으로 `res/score.csv`에 모델별 성능을 기록하고, 테스트 성능이 우수한 모델을 최종 모델 학습에 사용하였다.

여섯째, 위험 판단 로직이 예측 결과와 자연스럽게 연결된다. 최종 위험 점수는 입력 보증금이 예측 적정가에서 얼마나 벗어났는지와, 해당 매물이 일반적인 거래 패턴에서 얼마나 벗어났는지를 함께 반영한다. 즉 “가격상 비정상”과 “데이터 패턴상 비정상”을 동시에 고려하므로, 단순 가격 비교보다 현실적인 위험 판단이 가능하다.

일곱째, 서비스 구현 구조가 명확하다. `train_final_model.py`는 최종 모델과 보조 객체를 저장하고, `predict.py`는 입력 데이터를 모델 입력 형식으로 변환하여 예측을 수행하며, `frontend/app.py`는 사용자가 입력하고 결과를 확인하는 화면을 제공한다. 각 코드의 역할이 분리되어 있어 유지보수와 확장이 가능하다.

전체 논리 흐름은 다음과 같다.

```text
전세 계약 위험 문제 인식
→ 공공·거래 데이터 수집 및 정제
→ 지역·시점·주택유형별 주변 시세 feature 생성
→ 주변 시세 대비 적정 보증금 비율 예측
→ 예측 비율을 원 단위 적정 보증금으로 변환
→ 입력 보증금과 적정가 차이 계산
→ 이상치·군집 기반 위험도 보완
→ 최종 위험 점수 및 라벨 제공
→ 임차인·공공기관의 계약 전 의사결정 지원
```

### 4-9. 최종 모델 구성

최종 모델은 `res/final_model`에 저장되어 있다. 주요 산출물은 다음과 같다.

| 파일 | 설명 |
|---|---|
| `catboost_final.cbm` | 최종 CatBoost 모델 파일 |
| `artifacts.joblib` | 예측에 필요한 전처리기, 비지도학습 모델, 참조 통계 등 |
| `metadata.json` | 모델 설정, 성능, 특징 목록, 예측 구간 산출 정보 |

최종 모델은 단순히 회귀 예측만 수행하지 않는다. 예측 결과를 실제 서비스에서 활용할 수 있도록 다음 정보를 함께 제공한다.

- 주변 전세 중위가 추정값
- 예측 적정 보증금
- 95% 예측 하한·상한
- 입력 보증금과 적정가의 차이
- 고가 위험 점수 및 저가 이상 점수
- 군집 거리 기반 이상도
- IsolationForest 기반 이상도
- 최종 위험 라벨

### 4-10. 위험도 산출 방식

위험도는 가격 위험과 이상 패턴 위험을 결합하여 산출한다. 가격 위험은 사용자가 입력한 보증금이 예측 적정 보증금 및 예측 상한에 비해 얼마나 벗어나는지를 기반으로 계산한다. 예를 들어 입력 보증금이 예측 적정가보다 지나치게 높다면 고가 위험 점수가 상승한다. 반대로 지나치게 낮은 가격 또한 허위 매물 또는 비정상 조건일 가능성이 있으므로 저가 이상 점수로 반영한다.

이상 패턴 위험은 KMeans 군집 거리와 IsolationForest 이상치 점수를 활용한다. 일반적인 매물 군집에서 멀리 떨어져 있거나, 학습 데이터에서 드문 패턴을 보이는 매물은 이상 점수가 높아진다. 최종적으로 가격 위험과 이상 패턴 위험을 조합하여 0~100점의 위험 점수를 만들고, 점수 구간에 따라 안전·적정·주의·위험 라벨을 부여한다.

### 4-11. 웹 서비스 구현

웹 서비스는 Python 기반의 경량 로컬 서버로 구현하였다. 사용자는 별도의 복잡한 설치 없이 프로젝트 환경에서 다음 명령어로 서비스를 실행할 수 있다.

```powershell
python src\frontend\app.py
```

실행 후 브라우저에서 다음 주소로 접속한다.

```text
http://127.0.0.1:8501
```

화면에서는 사용자가 매물 정보를 입력하고, 제출 버튼을 누르면 모델 예측 결과가 같은 페이지에 표시된다. 결과 화면에는 예측 적정가, 위험 라벨, 가격 이상 점수, 95% 예측 구간 등이 카드 형태로 제공되며, 사용자는 입력한 보증금이 적정 범위 안에 있는지 즉시 확인할 수 있다.

[그림 삽입 위치 1] 서비스 입력 화면 캡처

[그림 삽입 위치 2] 예측 결과 화면 캡처

### 4-12. 구현 가능성

본 서비스는 이미 로컬 환경에서 실행 가능한 형태로 구현되어 있다. 데이터 전처리, 모델 학습, 최종 모델 저장, 예측 스크립트, 프론트엔드 화면까지 모두 코드로 구성되어 있으며, 결과 파일 또한 저장되어 있다. 따라서 단순 아이디어 제안이 아니라 실제 동작 가능한 프로토타입 단계에 도달한 상태이다.

향후 배포 단계에서는 현재 로컬 서버를 클라우드 환경 또는 공공기관 내부 시스템에 배포할 수 있다. 입력 폼은 웹 기반이므로 사용자는 별도 프로그램 설치 없이 브라우저에서 접근할 수 있으며, 모델 파일은 `catboost_final.cbm`과 `artifacts.joblib`로 분리되어 있어 재학습 및 업데이트도 가능하다.

### 4-13. 기대효과

첫째, 임차인의 정보 비대칭을 줄일 수 있다. 사용자는 매물 계약 전에 데이터 기반으로 적정 보증금 범위와 위험 라벨을 확인할 수 있으므로, 주변 시세를 충분히 알지 못해 과도한 보증금을 부담하는 문제를 줄일 수 있다.

둘째, 전세사기 및 깡통전세 예방에 기여할 수 있다. 본 서비스는 매매가, 전세가, 주변 거래량, 이상치 점수 등을 종합적으로 반영하므로 일반적인 단순 시세 조회보다 위험 신호를 더 입체적으로 제공할 수 있다.

셋째, 공공기관의 상담 및 심사 업무를 보조할 수 있다. 한국토지주택공사 등 주거복지 기관은 전세임대 지원, 주거 상담, 위험 매물 모니터링 과정에서 본 서비스를 참고 지표로 활용할 수 있다.

넷째, 지역별 주거 위험 분석에도 활용 가능하다. 개별 매물 단위의 위험 판단 결과가 누적되면, 특정 지역·주택유형·시점에서 위험 매물이 증가하는 흐름을 파악할 수 있다. 이는 정책 수립 및 현장 점검 우선순위 선정에 도움을 줄 수 있다.

---

## 5) 자유기재

### 5-1. 차별성

본 서비스의 차별점은 단순히 주변 시세를 보여주는 것이 아니라, 사용자가 입력한 매물 조건을 바탕으로 적정 보증금과 위험도를 동시에 제시한다는 점이다. 또한 지도학습 기반 가격 예측과 비지도학습 기반 이상 탐지를 결합하여, 가격 수준과 거래 패턴의 비정상성을 함께 판단한다.

기존 부동산 서비스가 매물 탐색과 가격 비교에 초점을 둔다면, 본 서비스는 계약 전 위험 판단이라는 실질적 의사결정 지원에 초점을 둔다. 특히 비전문가가 이해하기 쉽도록 복잡한 모델 결과를 금액, 범위, 점수, 라벨로 변환한 점이 특징이다.

### 5-2. 한계 및 개선 방향

현재 모델은 확보된 데이터 범위 안에서 학습되었기 때문에, 학습 데이터에 충분히 포함되지 않은 지역, 특수한 주택유형, 급격한 시장 변동 상황에서는 예측 불확실성이 커질 수 있다. 이를 보완하기 위해 서비스는 예측 구간과 모델 적용 범위 정보를 함께 제공한다.

향후 개선 방향은 다음과 같다.

- 최신 실거래 데이터의 주기적 업데이트
- 등기부등본, 근저당, 보증보험 가입 가능 여부 등 위험 판단 데이터 확장
- 지역별 전세가율 및 매매가 변동률 반영
- 사용자 입력값 오류 검증 기능 강화
- 지도 기반 위험 매물 분포 시각화
- 공공기관 상담 시스템과 연계한 의사결정 지원 기능 개발

### 5-3. 활용 시나리오

임차인은 계약 전 매물 정보를 입력하여 해당 매물이 적정 보증금 범위에 있는지 확인한다. 만약 현재 보증금이 예측 상한보다 높고 위험 점수가 높게 나타난다면, 임차인은 추가 확인을 진행하거나 계약 조건 재검토를 요청할 수 있다.

공공기관 상담 담당자는 전세임대 또는 주거 상담 과정에서 신청자가 제시한 매물의 위험도를 빠르게 확인할 수 있다. 이를 통해 고위험 매물에 대해서는 추가 서류 확인, 보증보험 가능 여부 확인, 대체 매물 안내 등을 수행할 수 있다.

지자체 또는 공공기관은 누적된 판단 결과를 지역별로 집계하여 위험 매물 가능성이 높은 지역이나 주택유형을 모니터링할 수 있다. 이는 예방 중심의 주거복지 정책과 현장 점검 체계 마련에 활용될 수 있다.

---

## 부록. AI 활용 결과 증빙

### 주요 실행 파일

| 경로 | 역할 |
|---|---|
| `src/data/preprocess.py` | 모델링 데이터 전처리 |
| `src/models/catboost_optuna.py` | CatBoost Optuna 튜닝 |
| `src/models/train_final_model.py` | 최종 모델 학습 및 artifact 저장 |
| `src/models/predict.py` | 입력 데이터 기반 예측 수행 |
| `src/frontend/app.py` | 로컬 웹 서비스 실행 |
| `src/models/supervised_ensemble.py` | 지도학습 모델 앙상블 |
| `src/models/dnn.py` | DNN 기반 회귀 모델 실험 |

### 주요 실행 명령 예시

```powershell
python src\models\catboost_optuna.py --n-trials 100
python src\models\train_final_model.py
python src\models\predict.py
python src\frontend\app.py
```

### 주요 결과 파일

| 경로 | 내용 |
|---|---|
| `res/catboost_optuna/metrics.json` | CatBoost Optuna 모델 성능 및 최적 파라미터 |
| `res/score.csv` | 모델별 성능 비교표 |
| `res/final_model/metadata.json` | 최종 모델 메타데이터 |
| `res/final_model/catboost_final.cbm` | 최종 CatBoost 모델 |
| `res/final_model/artifacts.joblib` | 예측 및 위험도 산출에 필요한 보조 객체 |

### 모델 성능 요약

최종 후보 모델인 CatBoost Optuna는 테스트 데이터에서 RMSE 0.1815, MAE 0.1308, R2 0.9309를 기록하였다. 이는 테스트 데이터 기준으로 예측 설명력이 높으며, 서비스 프로토타입에 적용 가능한 수준의 성능으로 판단된다.

### 제출 전 확인 사항

- 본문 내 [그림 삽입 위치]에 실제 웹 화면 캡처 삽입
- 팀명, 팀원명, 제출일 등 공모전 요구 정보 추가
- 데이터 출처의 공식 명칭 확인 후 활용 데이터 항목에 보완
- HWPX 양식의 안내 문구 삭제 후 제출
- 폰트 11pt, 줄간격 160%, 20페이지 이하 확인

---

## 부록. 코드별 활용 Feature 및 의미

### 1. 코드별 Feature 활용 방식

본 프로젝트의 대부분 모델은 `src/utils/modeling.py`의 `select_features()` 함수를 통해 공통 feature 목록을 구성한다. 이 함수는 타깃 변수와 누출 가능성이 있는 변수, 계약 후에야 알 수 있는 일부 금액 변수 등을 제외한 뒤, 범주형 변수와 수치형 변수를 분리한다. 이후 모델 특성에 따라 전처리 방식만 다르게 적용한다.

| 코드 | 모델/역할 | 사용 feature |
|---|---|---|
| `src/models/baseline.py` | 기준 모델 | 공통 feature. 범주형은 One-Hot Encoding, 수치형은 결측 대체 및 표준화 |
| `src/models/tree_models.py` | LightGBM, XGBoost, CatBoost 기본 비교 | 공통 feature. LGBM/XGBoost는 전처리 파이프라인, CatBoost는 범주형 변수를 직접 처리 |
| `src/models/optuna.py` | LightGBM Optuna 튜닝 | 공통 feature. Optuna로 LGBM 하이퍼파라미터 탐색 |
| `src/models/catboost_optuna.py` | CatBoost Optuna 튜닝 | 공통 feature + 조합형 범주 feature 5개 |
| `src/models/stacking.py` | Stacking 모델 | 공통 feature로 여러 base model을 학습하고, 각 모델 예측값을 meta model 입력으로 사용 |
| `src/models/supervised_ensemble.py` | 지도학습 앙상블 | 저장된 모델 예측값을 조합하여 equal/weighted ensemble 구성 |
| `src/models/dnn.py` | Tabular DNN | 공통 feature + 조합형 범주 feature. 범주형은 embedding, 수치형은 표준화 후 MLP 입력 |
| `src/models/train_final_model.py` | 최종 서비스 모델 학습 | CatBoost Optuna 최적 파라미터와 공통+조합 feature 사용. 추가로 비지도학습 feature/모델 생성 |
| `src/models/predict.py` | 최종 예측 | 사용자가 입력한 값을 최종 모델 feature 형식으로 변환하고 적정가·위험도 산출 |
| `src/frontend/app.py` | 웹 서비스 | 사용자 입력값을 `predict.py`에 전달하고 결과를 화면에 표시 |
| `src/utils/unsupervised_features.py` | 비지도 feature 생성 | 공통 feature를 전처리한 뒤 KMeans, IsolationForest, robust z-score 등 이상도 feature 생성 |

### 2. CatBoost Optuna 및 최종 모델의 주요 Feature

CatBoost Optuna와 최종 모델은 총 46개 feature를 사용한다. 이 중 11개는 범주형 feature, 35개는 수치형 feature이다.

#### 2-1. 범주형 Feature

| Feature | 의미 | 활용 이유 |
|---|---|---|
| `gu_code` | 자치구 코드 | 지역별 전세 가격 수준 차이 반영 |
| `gu_name` | 자치구명 | 자치구 단위 시장 특성 반영 |
| `ym` | 계약연월 | 시점별 전세 시장 변화 반영 |
| `property_type` | 표준화된 주택유형 | 아파트, 오피스텔, 연립/다세대 등 유형별 가격 차이 반영 |
| `주택유형` | 원천 데이터의 주택유형명 | 원본 분류 정보를 보존하여 세부 주택 특성 반영 |
| `유형` | 원천 데이터의 상세 유형 | 주택유형보다 더 세부적인 유형 정보 반영 |
| `gu_property` | 자치구 + 주택유형 조합 | 특정 지역의 특정 주택유형 가격 패턴 반영 |
| `gu_ym` | 자치구 + 계약연월 조합 | 지역별·월별 시장 흐름 반영 |
| `property_ym` | 주택유형 + 계약연월 조합 | 주택유형별 시기 변화 반영 |
| `housing_type_ym` | 원천 주택유형 + 계약연월 조합 | 세부 주택유형의 시기별 패턴 반영 |
| `detail_type_ym` | 상세 유형 + 계약연월 조합 | 상세 유형의 월별 가격 패턴 반영 |

#### 2-2. 수치형 Feature

| Feature | 의미 | 활용 이유 |
|---|---|---|
| `방갯수` | 원천 데이터의 방 개수 | 주거 규모 및 가격 수준과 관련 |
| `area_m2` | 전용면적 또는 계약 면적, 제곱미터 기준 | 면적은 보증금 수준을 결정하는 핵심 변수 |
| `세대원수` | 세대원 수 | 주거 수요 규모 및 주택 특성 보조 변수 |
| `rent_txn_count` | 동일 지역·시점·유형의 임대 거래 건수 | 주변 전세/월세 시세의 신뢰도 반영 |
| `jeonse_txn_count` | 동일 그룹의 전세 거래 건수 | 전세 시장 표본 크기 반영 |
| `rent_deposit_median` | 주변 임대 보증금 중위값 | 적정 전세 보증금 예측의 핵심 기준값 |
| `rent_deposit_mean` | 주변 임대 보증금 평균값 | 주변 시세 수준 보조 정보 |
| `rent_deposit_per_m2_median` | 면적당 임대 보증금 중위값 | 면적 차이를 보정한 주변 시세 |
| `rent_deposit_per_m2_mean` | 면적당 임대 보증금 평균값 | 면적 보정 시세 보조 정보 |
| `monthly_rent_median` | 주변 월세 중위값 | 전월세 시장 수준 및 대체 비용 반영 |
| `area_m2_median` | 주변 거래의 면적 중위값 | 동일 그룹의 일반적인 주택 규모 반영 |
| `sale_txn_count` | 동일 지역·시점·유형의 매매 거래 건수 | 매매 시세 신뢰도 반영 |
| `sale_price_median` | 주변 매매가 중위값 | 전세 보증금의 안정성 판단에 필요한 기준 |
| `sale_price_mean` | 주변 매매가 평균값 | 매매 시장 가격 수준 보조 정보 |
| `sale_price_per_m2_median` | 면적당 매매가 중위값 | 면적 차이를 보정한 매매 시세 |
| `sale_price_per_m2_mean` | 면적당 매매가 평균값 | 면적 보정 매매 시세 보조 정보 |
| `sale_area_m2_median` | 주변 매매 거래 면적 중위값 | 매매 표본의 규모 특성 확인 |
| `is_ood_property_type` | 학습 주요 범위 밖 주택유형 여부 | 모델 적용 범위 밖 입력인지 판단 |
| `is_in_domain_property_type` | 학습 주요 범위 내 주택유형 여부 | 정상 학습 대상 여부 표시 |
| `is_exact_duplicate` | 완전 중복 행 여부 | 데이터 품질 및 중복 위험 반영 |
| `area_missing_or_zero` | 면적 결측 또는 0 여부 | 면적 정보 신뢰도 반영 |
| `area_too_small` | 면적이 비정상적으로 작은지 여부 | 이상 입력 탐지 |
| `area_review_large` | 면적이 검토가 필요한 큰 값인지 여부 | 대형 면적 매물의 특수성 반영 |
| `area_too_large` | 면적이 비정상적으로 큰지 여부 | 이상 입력 탐지 |
| `room_missing` | 방 개수 결측 여부 | 방 개수 정보 신뢰도 반영 |
| `room_too_large` | 방 개수가 비정상적으로 큰지 여부 | 이상 입력 탐지 |
| `household_size_missing_or_zero` | 세대원 수 결측 또는 0 여부 | 세대원 수 정보 신뢰도 반영 |
| `sale_feature_missing` | 매매 시세 feature 결측 여부 | 매매 정보 부족으로 인한 불확실성 반영 |
| `sale_low_confidence` | 매매 거래량이 적어 신뢰도가 낮은지 여부 | 매매 시세의 표본 부족 위험 반영 |
| `rent_feature_missing` | 임대 시세 feature 결측 여부 | 주변 임대 시세 부족 여부 반영 |
| `area_m2_clean` | 비정상 면적을 결측 처리한 정제 면적 | 실제 예측에 사용할 안정적인 면적값 |
| `room_count_clean` | 비정상 방 개수를 결측 처리한 정제 방 개수 | 실제 예측에 사용할 안정적인 방 개수 |
| `room_count_imputed` | 주택유형별 대표값으로 보완한 방 개수 | 결측 방 개수 보완 |
| `household_size_clean` | 비정상 세대원 수를 결측 처리한 정제 세대원 수 | 실제 예측에 사용할 안정적인 세대원 수 |
| `household_size_imputed` | 주택유형별 중앙값 등으로 보완한 세대원 수 | 결측 세대원 수 보완 |

### 3. 타깃 변수와 제외 변수

모델의 타깃 변수는 `log1p_lh_vs_rent_median_ratio_winsor`이다. 이는 대상 매물 보증금이 주변 전세 중위가 대비 어느 정도 수준인지를 나타내는 비율을 이상치 완화 후 로그 변환한 값이다. 모델은 이 값을 예측한 뒤, 예측 비율을 주변 전세 중위가에 곱하여 적정 보증금 금액을 산출한다.

일부 변수는 모델 입력에서 제외하였다. 예를 들어 `deposit_won`, `support_won`, `tenant_deposit_won`, `monthly_rent_won`, `lh_vs_rent_median_ratio` 등은 타깃 산출과 직접적으로 연결되거나 계약 결과를 포함할 수 있어 데이터 누출 위험이 있다. 따라서 학습 feature가 아니라 타깃 생성, 평가, 사용자 입력 비교 등에 활용하였다.

### 4. 비지도학습 Feature

최종 서비스는 적정가 예측 외에 위험도 판단을 위해 비지도학습 feature도 사용한다.

| Feature | 의미 | 활용 방식 |
|---|---|---|
| `kmeans_distance` | 입력 매물이 소속 군집 중심에서 떨어진 거리 | 일반적인 매물 패턴에서 얼마나 벗어났는지 판단 |
| `isolation_score` | IsolationForest 기반 이상치 점수 | 학습 데이터에서 드문 패턴인지 판단 |
| `robust_z` | 강건한 표준화 기반 이상도 | 극단값 영향을 줄인 이상 정도 측정 |
| `cluster_id` | 유사 매물 군집 번호 | 사용자가 입력한 매물의 유사 그룹 파악 |
| `anomaly_percentile` | 이상도 백분위 | 위험 점수 산출에 활용 |

최종 위험도는 가격 위험과 이상 패턴 위험을 함께 고려한다. 즉 입력 보증금이 예측 적정가보다 과도하게 높거나 낮은지, 그리고 해당 매물이 일반적인 지역·유형·면적 패턴에서 벗어나는지를 동시에 판단한다.
